---
/**
 * BaseLayout.astro – Layout base do site TechAI
 * Define HTML base, meta SEO, Open Graph, Twitter Cards e JSON-LD.
 * Envolve conteúdo com Header e Footer.
 */
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import WhatsAppFloat from '../components/WhatsAppFloat.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  image?: string;
  noIndex?: boolean;
}

const { title, description, image = '/reference/2025-digilink-html-page-about.html/images/img-030.jpg', noIndex = false } = Astro.props;
const siteUrl = 'https://techai.vercel.app';
const fullTitle = title === 'TechAI' ? title : `${title} | TechAI`;
const fullDescription = description || 'Agência especializada em soluções de IA e tecnologia';
const fullImage = image.startsWith('http') ? image : `${siteUrl}${image.startsWith('/') ? '' : '/'}${image}`;
---

<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{fullTitle}</title>
    <meta name="description" content={fullDescription} />
    <link rel="canonical" href={`${siteUrl}${Astro.url.pathname || '/'}`} />
    {noIndex && <meta name="robots" content="noindex, nofollow" />}

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={fullDescription} />
    <meta property="og:url" content={Astro.url.href} />
    <meta property="og:image" content={fullImage} />
    <meta property="og:locale" content="pt_BR" />
    <meta property="og:site_name" content="TechAI" />

    <!-- Twitter Cards -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={fullDescription} />
    <meta name="twitter:image" content={fullImage} />

    <!-- JSON-LD -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "TechAI",
      "url": siteUrl,
      "logo": `${siteUrl}/reference/2025-digilink-html-page-about.html/images/img-001.png`,
      "description": fullDescription,
      "contactPoint": {
        "@type": "ContactPoint",
        "telephone": "+55-21-99521-1289",
        "contactType": "customer service",
        "availableLanguage": "Portuguese"
      }
    })} />
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "TechAI",
      "url": siteUrl,
      "potentialAction": {
        "@type": "SearchAction",
        "target": `${siteUrl}/blog?q={search_term_string}`,
        "query-input": "required name=search_term_string"
      }
    })} />
  </head>
  <body class="bg-[#0D0D0D] text-white antialiased">
    <Header />
    <main class="pt-16 md:pt-20">
      <slot />
    </main>
    <Footer />
    <WhatsAppFloat />
  </body>
</html>
