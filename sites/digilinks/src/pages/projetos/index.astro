---
/**
 * projetos/index.astro – Listagem de projetos (Prompt 3)
 * Base: 2025-digilink-html-page-projects.html
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageTitle from '../../components/PageTitle.astro';
const ref = '2025-digilink-html-page-projects.html';

const projetos = [
  { slug: 'cartao-visual', title: 'Design de cartão de visitas', tags: ['Branding', 'UI/UX', 'Design'] },
  { slug: 'scrappy', title: 'Scrappy & Resourceful', tags: ['Branding', 'UI/UX', 'Design'] },
  { slug: 'showcase', title: 'Mostre seu processo', tags: ['Branding', 'UI/UX', 'Design'] },
  { slug: 'onboarding', title: 'Onboarding profissional', tags: ['Branding', 'UI/UX', 'Design'] },
  { slug: 'cold-outreach', title: 'Cold outreach segmentado', tags: ['Branding', 'UI/UX', 'Design'] },
];

const images = ['img-005', 'img-006', 'img-007', 'img-008', 'img-009'] as const;
---

<BaseLayout title="Nossos Projetos" description="Portfólio de projetos da Digilinks: branding, UI/UX e design digital.">
  <PageTitle title="Nossos Projetos" breadcrumbs={[{ label: 'Início', href: '/' }, { label: 'Projetos' }]} image={`/reference/${ref}/images/img-013.jpg`} />

  <section class="py-16 md:py-24 px-4 bg-[#0f0f0f]">
    <div class="max-w-7xl mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {projetos.map((p, i) => (
          <a href={`/projetos/${p.slug}`} class="group block rounded-xl overflow-hidden bg-[#131212] border border-white/10 hover:border-[#D9F45F]/50 transition-all">
            <div class="aspect-[4/3] overflow-hidden">
              <img
                src={`/reference/${ref}/images/${images[i]}.jpg`}
                alt={p.title}
                class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
              />
            </div>
            <div class="p-6">
              <ul class="flex flex-wrap gap-2 mb-2">
                {p.tags.map((t) => (
                  <li class="text-xs text-[#D9F45F]">{t}</li>
                ))}
              </ul>
              <h3 class="font-bold text-white group-hover:text-[#D9F45F] transition-colors">{p.title}</h3>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
